# Cloud Run configuration
server.port=${PORT:8080}
server.address=0.0.0.0

# Google Cloud SQL PostgreSQL configuration
spring.cloud.gcp.sql.instance-connection-name=hello-world-472006:us-central1:amazon-products-1757760180
spring.cloud.gcp.sql.database-name=amazon_product

# Database credentials
spring.datasource.username=postgres
spring.datasource.password=Sasikala@2361

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# Connection pooling for Cloud Run
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1

spring.application.name=product-service
